# instalaciones
Instalaciones para jupyter notebook en linux y github ssh en linux

Despues de abrir nuestro ubuntu linux, copiar todas las lineas de código en orden y ver saltos de linea, salto de linea delimita si debes ejecutar el código no no

# instalación de Jupyter notebook
sudo apt update && sudo apt upgrade -y
sudo apt install python3 python3-pip python3-venv -y

# Creación de llave SSH desde linux para github
ssh-keygen -t rsa -b 4096 -C 'guayito.palom0@gmail.com'
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
cat ~/.ssh/id_rsa.pub
[Abrir github, copiar key y agregarla a github] bajo este link: https://github.com/settings/keys

# Configurar github con nuestro email y usuario
git config --global user.name "eduardo-Cubiasss"
git config --global user.email "guayito.palom0@gmail.com"

# Códigos de terminal y github para guiarte:
https://docs.google.com/document/d/1zyZKnMWr-DaI-4MpXwnWRaj0q7DteN0fU4ML9m27DWo/edit?usp=sharing

# Repositorio de apoyo para sintaxis o estructuras de control
git clone git@github.com:profesorIS2020/ipynbPrograI.git

# Códigos básicos de github: Hacerlo dentro de donde clonamos la carpeta
git pull origin main
git add .
git commit -m "Agregando"
git push origin main
